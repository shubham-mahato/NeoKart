<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NeoKart Account Activation</title>
    <style type="text/css">
      /* Base */
      body {
        margin: 0;
        padding: 0;
        min-width: 100%;
        font-family: Arial, sans-serif;
        font-size: 16px;
        line-height: 1.5;
        background-color: #fafafa;
        color: #222222;
      }
      a {
        color: #0070f3;
        text-decoration: none;
      }
      h1 {
        font-size: 24px;
        font-weight: 700;
        line-height: 1.25;
        margin-top: 0;
        margin-bottom: 15px;
        text-align: center;
      }
      p {
        margin-top: 0;
        margin-bottom: 24px;
      }
      table td {
        vertical-align: top;
      }

      /* Layout */
      .email-wrapper {
        max-width: 600px;
        margin: 0 auto;
        border: 1px solid #eaeaea;
        border-radius: 6px;
        overflow: hidden;
      }
      .email-header {
        background-color: #0070f3;
        padding: 24px;
        color: #ffffff;
        text-align: center;
      }
      .email-body {
        padding: 24px;
        background-color: #ffffff;
      }
      .email-footer {
        background-color: #f6f6f6;
        padding: 20px;
        text-align: center;
        font-size: 14px;
        color: #666666;
      }

      /* OTP Box */
      .otp-box {
        display: inline-block;
        background-color: #f0f4ff;
        color: #0070f3;
        font-size: 24px;
        font-weight: bold;
        padding: 12px 24px;
        border-radius: 6px;
        margin: 20px 0;
        letter-spacing: 4px;
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <!-- Header -->
      <div class="email-header">
        <h1>Welcome to neoKart</h1>
      </div>

      <!-- Body -->
      <div class="email-body">
        <p>Hello <%= name %>,</p>
        <p>
          Thank you for registering with <strong>neoKart</strong>. To activate
          your account, please use the following activation code:
        </p>

        <div class="otp-box"><%= otp %></div>

        <p>
          Please enter this code on the activation page within the next 5
          minutes.
        </p>

        <p>
          If you did not register for a neoKart account, please ignore this
          email.
        </p>
      </div>

      <!-- Footer -->
      <div class="email-footer">
        <p>
          If you have any questions, please reach out to us at
          <a href="mailto:support@neokart.com">support@neokart.com</a>
        </p>
        <p>© <%= new Date().getFullYear() %> neoKart. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
